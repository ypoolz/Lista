# Faça uma função chamada area que calcula de uma circunferência. Utilize o valor de π fornecido pelo módulo math.

def area(n):
    return area

area = float(input('Entre com o valor do raio, para obter a circuferência do círculo: '))

circunferencia = 2 * 3.14 * area

print("A circuferência do círculo: {:.2f}".format(circunferencia))
